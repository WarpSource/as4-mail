

== Security:

TODO:
For development reasons the
plain-text="true"
was set to ManagementRealm and LaurentiusRealm. For prod use the default format

    username=HEX( MD5( username ':' realm ':' password))

== Logging

add new logger

           <periodic-rotating-file-handler name="FILE_AS4AMAIL_UI" autoflush="true">
                <filter-spec value="match(&quot;SECURITY&quot;)"/>
                <formatter>
                    <named-formatter name="PATTERN"/>
                </formatter>
                <file relative-to="jboss.server.log.dir" path="as4mail-ui.log"/>
                <suffix value=".yyyy-MM-dd"/>
                <append value="true"/>
            </periodic-rotating-file-handler>

change the patterns to log sessio

            <formatter name="PATTERN">
                <pattern-formatter pattern="%d{yyyy-MM-dd HH:mm:ss,SSS} %-5p [%X{CLIENT_HOST}] [%X{REQUEST_ID}] [%X{SESSION_ID}] [%c{1.}] [%t] %s%e%n"/>
            </formatter>
            <formatter name="COLOR-PATTERN">
                <pattern-formatter pattern="%K{level}%d{HH:mm:ss,SSS} %-5p [%x] [%k] [%i] [%MARKER] [%X{CLIENT_HOST}] [%X{REQUEST_ID}] [%X{SESSION_ID}] [%c{1.}] [%t] %s%e%n"/>
            </formatter>